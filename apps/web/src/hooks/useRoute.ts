// import { useContext } from 'react';
// import WebsiteContext from '../contexts/WebsiteContext';

// const useRouteExists = (uid: string, slug: string) => {
// 	const { routes, locale } = useContext(WebsiteContext);

// 	if (!uid || !slug || !routes || !locale) {
// 		return null;
// 	}

// 	const route = routes[locale].filter((route) => route.slug === slug)[0];

// 	return route ? route.route : null;
// };

// export default useRouteExists;
